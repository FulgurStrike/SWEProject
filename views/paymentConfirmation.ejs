<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/output.css">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <% if (errorMessages.length) { %>
        <% errorMessages.forEach(msg => { %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
            <%= msg %>
          </div>
        <% }) %>
      <% } %>
      
      <% if (successMessages.length) { %>
        <% successMessages.forEach(msg => { %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
            <%= msg %>
          </div>
        <% }) %>
      <% } %>
      <nav class="bg-blue-600 text-white p-4">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
          <div class="text-xl"><%= siteName || 'placeholder' %></div>
          <ul class="flex space-x-6">
            <li><a href="/" class="hover:text-gray-300 font-bold underline"><%= home %></a></li>
            <li><a href="/help" class="hover:text-gray-300"><%= help %></a></li>
    
            <% if (!isLoggedIn) { %>
              <li><a href="/login" class="hover:text-gray-300"><%= login %></a></li>
              <li><a href="/signup" class="hover:text-gray-300"><%= signUp %></a></li>
            <% } else { %>
              <li>
                <a href="/logout" class="hover:text-gray-300"><%= logout %></a></li>
                <% } %>
          </ul>
        </div>
      </nav>

    <main class="max-w-3xl mx-auto mt-20 bg-white p-8 rounded-lg shadow-lg text-center">
        <h1 class="text-3xl font-bold <%= paymentStatus === 'completed' ? 'text-green-600' : 'text-red-600' %> mb-4"><%= title %></h1>
        <p class="text-xl mb-4"><%= message %></p>
        <div class="bg-gray-100 p-4 rounded-md">
            <p><strong>Parking Request ID:</strong> <%= parkingRequestID %></p>
            <p><strong>Amount:</strong> £<%= amount %></p>
            <p><strong>Payment Status:</strong> <%= paymentStatus %></p>
        </div>
        <div class="mt-6">
            <a href="/" class="bg-blue-600 text-white px-6 py-3 rounded-md hover:bg-blue-700">Back to Home</a>
        </div>
    </main>

    <footer class="bg-blue-600 text-white py-6">
        <div class="max-w-7xl mx-auto text-center">
            <p>&copy; Parking System</p>
        </div>
    </footer>
</body>
</html>
